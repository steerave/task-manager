# Task Manager - the rules are not in this file

Hermes loads exactly one project context file and this is it, so this file only says where to look.

**Before you do anything in this project, read `CLAUDE.md` at the repository root, in full.**
It is about 96 lines and it holds every rule that stops a mistake being made here.

A personal command-line task manager that stores tasks as markdown in an Obsidian vault.

`docs/status.md` says what was last done, what is in progress and what is next. Read it before picking work up.

This pointer exists because Hermes reads one context file, first match wins, and it has no import mechanism.
AgentOS RULEBOOK rule 4.28.
